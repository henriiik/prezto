#
# Executes commands at the start of an interactive session.
#
# Authors:
#   Sorin Ionescu <sorin.ionescu@gmail.com>
#

# Source Prezto.
if [[ -s "${ZDOTDIR:-$HOME}/.zprezto/init.zsh" ]]; then
  source "${ZDOTDIR:-$HOME}/.zprezto/init.zsh"
fi

# only branches and files git completion
__git_files () {
  _wanted files expl 'local files' _files
}

alias j=fasd_cd

# add Pulumi to the PATH
export PATH=$PATH:$HOME/.pulumi/bin

WHATCOMPUTERISTHIS=$(hostname)

if [[ "$WHATCOMPUTERISTHIS" = "DESKTOP-J25URBS" ]]; then
  echo "detected DESKTOP-J25URBS!"
  export PNPM_HOME="/home/henro/.local/share/pnpm"
  export PATH="$PNPM_HOME:$PATH"
  eval `keychain --eval --agents ssh id_ed25519`
elif [[ "$WHATCOMPUTERISTHIS" = "bappy.localdomain" ]]; then
  echo "detected bappy!"
  export ASDF_DATA_DIR=`brew --prefix asdf`
  source $ASDF_DATA_DIR/asdf.sh
elif [[ "$WHATCOMPUTERISTHIS" = "happy.local" ]]; then
  echo "detected happy!"
  export ASDF_DATA_DIR=`brew --prefix asdf`
  source $ASDF_DATA_DIR/asdf.sh
elif [[ "$WHATCOMPUTERISTHIS" == dappy* ]]; then
  echo "detected dappy!"
  export GODEBUG=asyncpreemptoff=1
  eval "$(/opt/homebrew/bin/brew shellenv)"
  if [[ -s "$HOME/.asdf/asdf.sh" ]]; then
    . $HOME/.asdf/asdf.sh
  fi
  . "$HOME/.cargo/env"
  eval "$(starship init zsh)"
elif [[ "$WHATCOMPUTERISTHIS" == codespaces_* ]]; then
  echo "detected codespace!"
  if [[ -s "$HOME/.asdf/asdf.sh" ]]; then
    . $HOME/.asdf/asdf.sh
  fi

  export AWS_VAULT_BACKEND=file

  VAULT_KEY_DIR=$HOME/.awsvault/keys
  VAULT_KEY_FILE=$VAULT_KEY_DIR/codespace
  rm -f $VAULT_KEY_FILE # remove file if it has an older version
  mkdir -p $VAULT_KEY_DIR # make sure key dir exists
  echo -n $AWS_VAULT_CODESPACE > $VAULT_KEY_FILE

  AWS_CONFIG_DIR=$HOME/.aws
  AWS_CONFIG_FILE=$AWS_CONFIG_DIR/config
  rm -f $AWS_CONFIG_FILE # remove file if it has an older version
  mkdir -p $AWS_CONFIG_DIR # make sure config dir exists
  echo "[default]\nregion = eu-north-1\n\n[profile codespace]" > $AWS_CONFIG_FILE
elif [[ "$GITHUB_CODESPACE_TOKEN" ]]; then
  echo "detected codespace!"
  eval "$(starship init zsh)"
  eval "$(direnv hook zsh)"
else
  echo "detected unknown hostname: $WHATCOMPUTERISTHIS"
fi

alias p="pulumi"
alias psl="pulumi stack ls"
alias pss="pulumi stack select"
alias pso="pulumi stack output"
alias pu="pulumi up"
alias pp="pulumi preview"

alias gbl="git branch --sort=-committerdate --verbose"

alias wip='git add -A && git commit -m "wip"'
alias gfmm='git fetch origin master:master'
alias gmb='git merge-base'
alias gmbc='git merge-base "$(git-branch-current 2> /dev/null)" master'
alias gwrmb='git reset $(git merge-base "$(git-branch-current 2> /dev/null)" master)'
alias grimb='git rebase --interactive $(git merge-base "$(git-branch-current 2> /dev/null)" master)'
alias gwH='git reset --hard "origin/$(git-branch-current 2> /dev/null)"'
alias gwh='git reset --soft "origin/$(git-branch-current 2> /dev/null)"'

alias trashn="find . -name node_modules -type d -prune -exec trash {} +"

alias avebicd="KUBECONFIG=~/kubeconfig/bci-clearing-dev-main.json aws-vault exec bci-clearing-dev"
alias avebici="KUBECONFIG=~/kubeconfig/bci-clearing-int-main.json aws-vault exec bci-clearing-int"
alias avebicp="KUBECONFIG=~/kubeconfig/bci-clearing-prod-main.json aws-vault exec bci-clearing-prod"

alias avebxcd="KUBECONFIG=~/kubeconfig/bcx-clearing-dev-main.json aws-vault exec bcx-clearing-dev"
alias avebxci="KUBECONFIG=~/kubeconfig/bcx-clearing-int-main.json aws-vault exec bcx-clearing-int"
alias avebxcp="KUBECONFIG=~/kubeconfig/bcx-clearing-prod-main.json aws-vault exec bcx-clearing-prod"

# alias avenicd="KUBECONFIG=~/kubeconfig/ngc-clearing-dev-main.json aws-vault exec ngc-clearing-dev"
alias avenici="KUBECONFIG=~/kubeconfig/ngc-clearing-int-main.json aws-vault exec ngc-clearing-int"
alias avenicp="KUBECONFIG=~/kubeconfig/ngc-clearing-prod-main.json aws-vault exec ngc-clearing-prod"

# alias avenxcd="KUBECONFIG=~/kubeconfig/ngx-clearing-dev-main.json aws-vault exec ngx-clearing-dev"
alias avenxci="KUBECONFIG=~/kubeconfig/ngx-clearing-int-main.json aws-vault exec ngx-clearing-int"
alias avenxcp="KUBECONFIG=~/kubeconfig/ngx-clearing-prod-main.json aws-vault exec ngx-clearing-prod"
